# In Python, a set is a built-in data type that represents an unordered collection of 
# unique and immutable elements.

group = {95, 98, 97, 97, 97}
print(group)  # store unique
# print(group[0])  we can't access by index as it is unordered

for item in group:
    print(item)